# Build / local tester output
*.ex5
*.log
__pycache__/
*.pyc

# Local secrets (Docker Compose, future Python DSN)
.env
cts_ml/.env
configs/.env

# Phase 3 — generated datasets (local only)
cts_ml/data/

# Phase 3 Week 5 — exported model bundles (joblib + manifest; regenerate locally)
cts_ml/exports/

# Phase 4 API local env
cts_ml/phase4_api/.env
