# Optional: copy to `.env` in this directory to override defaults from docker-compose.yml.
# Docker Compose reads `.env` for variable substitution only; never commit real secrets.

POSTGRES_USER=cts_user
POSTGRES_PASSWORD=change_me_local_only
POSTGRES_DB=ctsdb
POSTGRES_PORT=5432
