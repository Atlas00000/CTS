# Phase 2 Week 5 — CSV → PostgreSQL loader (host Python 3.11+).
psycopg[binary]>=3.2.0,<3.3

# Phase 3 Week 2 — build_dataset.py (Parquet + QC)
pandas>=2.2.0,<3
pyarrow>=16.0.0,<20

# Phase 3 Week 3 — train_baseline.py (walk-forward split + sklearn)
scikit-learn>=1.4.0,<2
PyYAML>=6.0.0,<7
joblib>=1.3.0,<4

# Phase 3 Week 4 — train_booster.py (XGBoost)
xgboost>=2.1.0,<3
